<main>

<section class="py-5 text-center container">
  <div class="row py-lg-5">
    <div class="col-lg-6 col-md-8 mx-auto">
      <h1 class="fw-light">Welcome to SnapSpot</h1>
      <% if user_signed_in? %>
        <h2 class="lead text-muted">
          Hello　<%= link_to "#{current_user.nickname}", root_path, class: :greeting__link %>
        </h2>
      <% end %>
      <p class="lead text-muted">
      SnapSpotは、画像と撮影場所を共有するためのアプリです。何気ない写真を共有し、他のユーザーとの交流を楽しむことができます。</p>
      <p>
        <% if user_signed_in? %>
          <div class="nav__right">
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: :"btn btn-primary my-2" %>
            <%= link_to "新規投稿", new_post_path, data: {"turbolinks" => false}, class: :"btn btn-secondary my-2" %>
          </div>
        <% else %>
          <div class="nav__right">
            <%= link_to "Login", new_user_session_path, class: :"btn btn-primary my-2" %>
            <%= link_to "Sign up", new_user_registration_path, class: :"btn btn-secondary my-2" %>
          </div>
        <% end %>
      </p>
    </div>
  </div>
</section>

<div class="album py-5 bg-light">
  <div class="container">
    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
      <%= render partial: "post", locals: { collection: @post } %>
    </div>
  </div>
</div>

</main>

<footer class="text-muted py-5">
<div class="container">
  <p class="float-end mb-1">
    <a href="#">Back to top</a>
  </p>
  <p class="mb-1">Album example is &copy; Bootstrap, but please download and customize it for yourself!</p>
</div>
</footer>