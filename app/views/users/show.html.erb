<main>
  <section class="py-5 text-center container">
    <div class="col-lg-6 col-md-8 mx-auto">

      <!-- ユーザー名 -->
      <div class="h4 my-3">
        <%= "#{@user.nickname}さんの投稿"%>
      </div>
      <!-- /ユーザー名 -->

      <!-- spot一覧表示 -->
      <%= render partial: "posts/spot_list", locals: { collection: @post } %>
      <!-- spot一覧表示 -->

      <!-- ワード検索フォーム -->
      <%= form_with(url: search_posts_path, local: true, method: :get, class: "d-flex") do |f| %>
        <%= f.text_field :keyword, placeholder: "タイトルまたは投稿内容で撮影スポットを探す", class: "form-control me-2" %>
        <%= f.submit "Search", class: "btn btn-secondary" %>
      <% end %>
      <!-- /ワード検索フォーム -->
    </div>
  </section>

  <%= render partial: "posts/post", locals: { collection: @post } %>
  <div class="d-flex justify-content-center mt-3">
    <%= paginate @posts %>
  </div>
</main>